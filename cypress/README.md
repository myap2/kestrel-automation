# Cypress

TypeScript + Cypress 13. Page objects in `cypress/support/pages/`, intercepts in `cypress/e2e/mocked-catalog.cy.ts`.

Start the shop first (`npm run app` from the repo root).

```bash
npm ci
npx cypress run          # headless
npx cypress open         # interactive
```
